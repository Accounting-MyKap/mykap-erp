{
  "name": "mykap-erp",
  "version": "1.0.0",
  "description": "MyKap ERP - Loan Management System",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm install && npm run build && cp -r dist/* ../ 2>/dev/null || xcopy dist\\* ..\\ /E /I /Y 2>nul || robocopy dist .. /E",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
